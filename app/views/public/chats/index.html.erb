<table class="rooms">
  <tbody>
    <tr>
      <td>
        <% @chat_partners.each do |chat_partner| %>
          <%= link_to "#{chat_partner.first_name}#{chat_partner.last_name}さんとのトークルーム","/chats/#{chat_partner.id}" %>
        <% end%>
      </td>
    </tr>
  </tbody>
</table>